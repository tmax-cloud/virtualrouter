apiVersion: virtualrouter.tmax.hypercloud.com/v1
kind: NATRule
metadata:
  name: test2
  namespace: virtualrouter
spec:
  rules:
  - match:
      srcIP: 10.10.10.0/24
      protocol: icmp
    action:
      srcIP: 0.0.0.0
    
    
